<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <!--
            APP_ENV must be exactly `testing`: Laravel's ValidateCsrfToken (and
            several other guards) bypass themselves via Application::runningUnitTests(),
            which compares the environment against `testing` literally. Any other
            value leaves CSRF enforcement on and every POST/PATCH/DELETE test gets
            a 419 instead of reaching the controller.

            DB_* is pinned to an in-memory SQLite database so RefreshDatabase can
            never migrate:fresh a real database.

            DB_URL must be blanked, and this is not optional. Every connection in
            config/database.php reads `'url' => env('DB_URL')`, and
            ConfigurationUrlParser::parseConfiguration() overrides the driver from
            the URL scheme (postgresql -> pgsql) whenever that value is truthy.
            A populated DB_URL in .env therefore beats DB_CONNECTION and
            DB_DATABASE above, and `php artisan test` would point RefreshDatabase
            at the production Postgres database and migrate:fresh it away. The
            parser short-circuits on a falsy url (`if (! $url) return $config;`),
            so an empty value restores the SQLite pinning.
        -->
        <env name="APP_ENV" value="testing"/>
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="DB_URL" value=""/>
        <env name="APP_MAINTENANCE_DRIVER" value="file"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="BROADCAST_CONNECTION" value="null"/>
        <env name="CACHE_STORE" value="array"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <env name="NIGHTWATCH_ENABLED" value="false"/>
    </php>
</phpunit>
