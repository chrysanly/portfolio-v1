APP_NAME="CJ Roma Portfolio"
APP_ENV=test
APP_KEY=
APP_DEBUG=true
APP_URL=http://localhost

# RULES §7: tests run on the same engine as production, never SQLite.
# Create the schema once:  CREATE DATABASE cj_portfolio_test;
DB_CONNECTION=mysql
DB_HOST=127.0.0.1
DB_PORT=3306
DB_DATABASE=cj_portfolio_test
DB_USERNAME=root
DB_PASSWORD=

BCRYPT_ROUNDS=4
CACHE_STORE=array
MAIL_MAILER=array
QUEUE_CONNECTION=sync
SESSION_DRIVER=array
BROADCAST_CONNECTION=null

# Overwritten per test by PortfolioAdminTestCase; present so config() has a key.
PORTFOLIO_ADMIN_PIN_HASH=
